[pytest]
asyncio_mode = auto
testpaths = tests
pythonpath = .
addopts = -q --cov=core --cov=routers --cov-report=term-missing

[coverage.run]
source = core, routers
omit = tests/*

[coverage.report]
fail_under = 75
